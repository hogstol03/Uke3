export class Flashcard {
    constructor(id, question, answer, category, difficulty) {
        this.id = id;
        this.question = question;
        this.answer = answer;
        this.category = category;
        this.difficulty = difficulty;
    }
}
